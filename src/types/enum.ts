/* eslint-disable no-shadow */
export enum OrderStatus {
    pending = 'pending',
    canceled = 'canceled',
    confirmed = 'confirmed',
    shipping = 'shipping',
    delivered = 'delivered',
    done = 'done',
}
